# M3 — native Q2_K indexed down projection

Decision: **pass.** Proceed later to dense Q8/wo_a and the graph-captured layer slice.

- Exact TP4 shape: six slot-specific K512 activations → N4096 partial outputs/rank.
- Native Q2_K×Q8_1 DP4A follows pinned llama.cpp/DwarfStar scale+min math; raw GGUF blocks, current stream, caller-owned buffers, CUDA-graph safe.
- RTX 3090: 5/5 numerical/graph tests pass against independently dequantized Q2_K×Q8 reference.
- Exclusive five-trial protocol: 5K warm/10K measured, GPU0 only, ≤1 process/sample, max clock 1650, canonical final zero-swap.

| Metric | Result |
|---|---:|
| down mean | **13.752 µs** |
| range | 13.696–13.790 µs |
| CV | 0.270% |
| logical bandwidth | **300.23 GB/s** |
| range | 299.40–301.45 GB/s |
| captured quantize+down | **14.898 µs** (0.430% CV) |

Combined expert projection estimate per layer: IQ2 gate+up captured pipeline 27.309 µs + Q2 down captured pipeline 14.898 µs = **42.207 µs**, competitive with the ~50 µs/layer Humming expert anchor from M0. This supports continuing; only the real graph-captured layer slice can make the final keep/stop decision.

Evidence: `evidence/m3-q2/`.
